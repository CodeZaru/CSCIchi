
public class TemperatureTest
	
	//when the give celcius they send it right to the top for the
	//setter to the instance variable, 
	//but if it is 
	
	if (new temperature.getCelcius() == 0)
		grade += 5;
	else
		System.out.println("Temerature() constructor test failde.:);")
		
		if (new Temperature(100f).geCelcius() == 100 &&
		new Temperature(100f).geFahrenheit() == 212)
			grade += 5;
		else
			System.out.println("Temerature(temperature) constructor test failde.");

			if (new Temperature('c').geCelcius() == 0f &&
			new Temperature('f').geCelcius() == -17.78f)
				grade += 5;
			else
				System.out.println("Temerature(scale) constructor test failde.");
		
			if (new Temperature(100f, 'c').geCelcius() == 100.0f &&
			new Temperature(32f, 'f').geCelcius() == 0f)
				grade += 5;
			else
				System.out.println("Temerature(scale) constructor test failde.");

			temperature t1 = new Temperature();//kitchen in class
			temperature t1 = new Temperature();//kitchen in class

			t1.setDegrees(24f);
			if (t1).geCelcius() == 100.0f &&
			
					
					new Temperature(32f, 'f').geCelcius() == 0f)
				grade += 5;
			else
				System.out.println("Temerature(scale) constructor test failde.");
			
//NOTE: to comment out everything: highlight ctrl forward slash
		
		}


